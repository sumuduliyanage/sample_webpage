apiVersion: v1
kind: Service
metadata:
  name: index-web-service
  labels:
    name: index-web-service
    app: demo-web-page
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30004
  selector:
    name: index-web-pod
    app: demo-web-page
